# 📈 AutoForecast — Модуль автоматизированного прогнозирования временных рядов

**AutoForecast** — это модуль для автоматического и ручного прогнозирования временных рядов, включающий:
- фильтрацию и подготовку данных,
- автоматический подбор параметров,
- псевдовневыборочные тесты с MAPE,
- выбор оптимальной модели для каждого шага прогноза,
- конструирование комбинированного прогноза,
- набор готовых моделей прогнозирования,
- отдельный движок для custom_ARIMA.

Проект позволяет:
- запускать **полностью автоматический прогноз**,
- использовать **каждую модель отдельно**, как в классической библиотеке.

---

## 📁 Структура проекта

```
AUTO_FORECAST/
│
├── engin.py                # Основной движок: все функции автоматизации
│                           # Auto_forecast(), Auto_params_selection(),
│                           # MAPE_step_by_step(), Time_Serial_filter(),
│                           # Filter_for_dataset(), Psevdo_forecast_test_MAPE()
│
├── models.py               # Все базовые прогнозные модели
│
├── get_arima.py            # Движок для custom_ARIMA
│
├── Example.ipynb           # Примеры использования
├── testing.ipynb           # Тестирование
│
├── test_data_10000.json    # Пример набора данных
│
└── README.md
```

---

## 🚀 Основные модули

### 🔹 engin.py — ядро AutoForecast
Содержит:
- Auto_forecast()
- Psevdo_forecast_test_MAPE()
- Auto_params_selection()
- MAPE_step_by_step()
- Time_Serial_filter()
- Filter_for_dataset()

### 🔹 models.py — модели:
- ps_RW, ps_RWS, ps_RWD, ps_RWDS, ps_TS, ps_ARIMA, ps_custom_ARIMA - Для построения псевдовневыборочных прогнозов.
- RW_real, RWS_real, RWD_real, RWDS_real, TS_real, ARIMA_real, custom_ARIMA_real - Для построения реальных прогнозов.

### 🔹 get_arima.py
Логика custom_ARIMA + вспомогательные функции.

---

## 🧩 Формат входных данных

```python
Data = {
    "observations": [
        {"date": "01.01.2010", "obs": 123.4},
        {"date": "01.02.2010", "obs": 125.1},
    ],
    "pandas_frequency": "ME",
    "exceptions": []
}
```

---

## 🔧 Пример: автоматический прогноз

```python
from engin import Auto_forecast
results = Auto_forecast(Data)
print(results)
```

## 🔧 Пример: отдельная модель

```python
from models import RW_real
forecast = RW_real(Data, Forecast_horizon=6)
```

## 🔧 Пример: псевдовневыборочный тест

```python
from engin import Psevdo_forecast_test_MAPE
models = Psevdo_forecast_test_MAPE(Data, 24, 12, 12, True)
```

---




